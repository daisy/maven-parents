#!/bin/bash

if [ "$TRAVIS_BRANCH" == "master" ] || [[ "$TRAVIS_BRANCH" == super/* ]] && [ "$TRAVIS_PULL_REQUEST" == "false" ]; then
  mvn deploy -B -DskipTests -Dinvoker.skip=true
else
  echo "Skipping Maven deploy."
fi
